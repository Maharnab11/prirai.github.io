@import url(https://cdn.jsdelivr.net/gh/tonsky/FiraCode@5.2/distr/fira_code.css);

:root {
	--colgr: #5A83B8;
	--colwh: #AFCBF0;
	--colbd: #0A346B;
	--colbl: #0E4D9E;
	--colpu: #1572EB;
	}

code {
	font-family: 'Fira Code', serif, monospace; 
	}

@supports (font-variation-settings: normal) {
  code { font-family: 'Fira Code VF', monospace; }
}

@media screen and (min-width: 1000px)
{
    body {
      font-family: 'Fira Code';
      background-image: url("images/wallp.webp");
      background-attachment: fixed;
      max-height: 100vh;
      max-width: 100vw;
    }

    #fixed-top {
        position: fixed;
        background-color:var(--colbl);
        width: 80%;
        top: 10px;
        text-align:center;
        transition: 0.7s;
        z-index: 1;
        box-shadow:
          rgba(90,83,184,0.4)
          10px 10px;

        border-radius: 0em 2em 3em 4em;
        border-color: var(--colwh);
        border-width: 0.8em;
        border-top-width: 0em;
        border-left-width: 0em;
        border-style: solid;

        margin-right: 10%;
        margin-left: 10%;
        }
        
        .main-title {
        	position: fixed; 
        	width: 100%; 
        	top: 1%;
        	font-size: 1000%; 
        	position: fixed; 
        	text-align: center; 
        	font-style: bold; 
        	color: white; 
        	z-index: 1;
        	}

    nav ul {
      margin: 0px;
      padding: 5px 0px 5px 30px;
    }

    nav li {
      display: inline-table;
      margin-right: 70px;
    }
    /*------------------------------------Links Styling----------------------------------------------------------------------*/
      a:link {
        color: var(--colgr);
        text-decoration: none;
      }

      a:visited {
        color: var(--colpu);
      }

      a:hover {
        background-color: var(--colpu);
        color: var(--colwh);
        text-decoration: underline;
      }

      li a:link {
        color: var(--colwh);
        border-color: var(--colbd);
        text-decoration: none;
      }

      li a:visited {
        color: var(--colwh);
        border-color: var(--colbd);
        text-decoration: solid;
      }

      li a:hover {
        color: var(--colbd);
        background-color: var(--colwh);
        text-decoration: dashed;
        text-decoration-color: var(--colbd);
      }

      .pg-active {
        color: var(--colbd);
        background-color: var(--colwh);
      }

      li a:active {
        color: yellow;
        border-color: var(--colwh);
        text-decoration: line-through;
        transition: 0.5s;
      }

      li {
        font-size: 160%;
      }

/*----------------------------------------------------------------------------------------------------------------------------------*/


    .bu:hover {
      background-color: var(--colwh);
    }

    iframe {
      border-radius: 2%;
    }

    .mcard {
      contain: content;
      z-index: 2;
      position:relative;
      margin-top: 300px;
      min-height: 50em;
      border-radius: 1em;
      background-color: var(--colwh);
      box-shadow:
           inset 0 -3em 3em rgba(0,0,0,0.2),
                 0 0  0 4px rgb(255,255,255),
                 0.3em 0.3em 1em rgba(0,0,0,0.5);
    }

    .Index {
      color: #2644FF;
      text-align: center;
      font-size: large;
    }

    header {
      color: var(--colwh);
      font-family: 'Fira Code';
      text-align: center;
      width: 100%;
    }

      .main-text {
        width: 70%;
        padding: 2% 20% 10% 20%;
        color: var(--colbd);
        font-size: 200%;
        font-weight: 300;
      }

      .largeimg {
        margin: 0px 10px 10px 10px;
        max-width: 80%;
        max-height: 70%;
        border-radius: 10%;
        border-color: var(--colbl);
        border-style: solid;
        border-width: 0.5em;
        height: 25em;
      }

      .round-img-box {
        display: list-item;
        contain: content;
        background-color: var(--colgr);
        border-radius: 10%;
        text-align: center;
        padding: 3% 10% 3% 10%;
      }

      .round-img {
        border-radius: 50%;
        border-width: 2em;
        padding: 0% 5% 0% 5%;
        opacity: 0.5;
      }

      .round-img:hover {
        border-width: 2em;
        border-radius: 10%;
        animation: shake 0.5s;
        opacity: 1;
      }

      .upper-text {
        font-size: 3em;
      }

      .schedule {
        position: fixed;
        top: 0%;
        border-color: var(--colgr);
        border-radius: 0.4em;
        border-width: 0.1em;
        border-style: dashed;
        color: var(--colwh);
        font-weight: 100;
        text-align: left;
        z-index: 99;

        height:100%;
        color: var(--colbd);
        min-width: 30%;
        background-color: black;
      }

      #sc-left {
        left: 0%;
        margin-right: 2%;
      }

      #sc-mid {
        left:32%;
        max-width: 36%;
        margin-right: 2%;
      }

      #sc-right {
        left:70%;
      }


      .inp-box {
        background-color: var(--colbd);
        border: none;
        border-radius: 50%;
        width: 60%;
        color: var(--colwh);
        margin: 1% 2% 1% 2%;
        word-wrap: break-word;
        outline: none;
        display: inline;
        flex: 100%;
        height: 10%;
        font-size: medium;
        box-shadow:  var(--colbd);
        border-color: var(--colwh);
        font-size: x-large;
        border-width: 1em;
        border-bottom-width: 0.3em;
        border-style: solid;
        text-align: center;
			}
    }

      @media only screen and (max-width: 1000px) {
        body {
          font-family: 'Fira Code';
          background-color: var(--colgr);
          padding: 0em;
          min-height: 100%;
          margin: 0em;
          max-width: 100%;
        }

        #fixed-top {
          background-color:var(--colbl);
          border-bottom-left-radius: 20%;
          border-bottom-right-radius: 20%;
          border-width: 0em;
          top: 0px;
          left: 0px;
          width: 100%;
          z-index: 1;
          padding: 0px;
          text-align: center;
        }
        
        .main-title {
        	position: fixed; 
        	width: 100%; 
        	top: 1%;
        	font-size: 1000%; 
        	position: fixed; 
        	text-align: center; 
        	font-style: bold; 
        	color: white; 
        	z-index: 1;
        	}

        nav ul {
          margin: 0px;
          padding: 0px 0px 5px 30px;
          width: auto;
        }

        nav li {
          display: contents;
          margin-right: 0px;
        }

        header {
          color: var(--colwh);
          font-family: 'Fira Code';
          text-align: center;
          font-size: 280%;
        }

        .mcard{
          background-color: black;
          position: relative;
          color: white;
          margin-top: 80%;
          padding-top: 20%;
          min-height: 200%;
          border-radius: 0.4em;
          z-index: 2;
        }

        .main-text {
        	color: white;
        padding: 2% 2% 10% 2%;
        font-size: 200%;
        font-weight: 300;
      }
      
      iframe {
        border-radius: 2%;
        width: 100%;
      }

      nav ul {
        margin: 0px;
        padding: 5px 0px 5px 30px;
      }

      nav li {
        display: inline-table;
        margin-right: 0px;
      }
      a:link {
        color: var(--colgr);
        text-decoration: none;
      }

      a:visited {
        color: var(--colpu);
      }

      a:hover {
        background-color: var(--colpu);
        color: var(--colwh);
        text-decoration: blink;
      }

      li a:link {
        color: var(--colwh);
        border-color: var(--colbd);
        text-decoration: none;
      }

      li a:visited {
        color: var(--colwh);
        border-color: var(--colbd);
        text-decoration: solid;
      }

      li a:hover {
        color: var(--colbd);
        background-color: var(--colwh);
        text-decoration: dashed;
        text-decoration-color: var(--colbd);
      }

      .pg-active {
        color: var(--colbd);
        background-color: var(--colwh);
      }

      li a:active {
        color: yellow;
        border-color: var(--colwh);
        text-decoration: line-through;
        transition: 0.5s;
      }

      li {
        font-size: 160%;
      }

      #embedded-feed {
        visibility: hidden;
      }

      .schedule {
        font-weight: 100;
      }

      .p-light-text {
        color : var(--colwh);
      }


     .inp-box {
       background-color: var(--colbd);
       color: var(--colwh);
       word-wrap: break-word;
       box-shadow:
       var(--colbd);
		
		margin: 2% 2% 2% 2%;
		min-width: 90%;
    	border-color: var(--colwh);
     	border-width: 0.5em;
     	border-bottom-width: 0.3em;
     	border-style: groove;
     	text-align: center;
     	font-size: 1em;
     	}
	}
